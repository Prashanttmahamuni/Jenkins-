pipeline {
    agent any

    stages {
        stage('Pull') {
            steps {
                echo 'Pull succeed'
            }
        }

        stage('Build') {
            steps {
                echo 'Build succeed'
            }
        }

        stage('Test') {
            steps {
                echo 'Test succeed'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy succeed'
            }
        }
    }
}
